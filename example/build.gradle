apply plugin: 'application'
apply plugin: 'java'
apply plugin: 'kotlin'

mainClassName = "${rootProject.group}.example.MainKt"

dependencies {
    compile rootProject
    compile "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    compile "com.importre:crayon:$crayon_version"
}
